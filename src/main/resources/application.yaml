server:
  port: 8888
spring:
  profiles:
    active: dev
  application:
    name: Wallet
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/user_wallet?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: 19990802
mybatis:
  mapper-locations: classpath:/mybatis/mapper/*.xml
  type-aliases-package: com.cheng.wallet.dto
